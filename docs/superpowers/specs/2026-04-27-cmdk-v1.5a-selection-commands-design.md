# Design: cmdk palette v1.5A - Selection commands without secondary pickers

## Goal

Add the first selection-aware command slice to the global cmdk palette.
When the user has selected assets, cmdk should expose the common bulk
asset verbs that are already present in the selection toolbar.

This is intentionally v1.5A, not the whole v1.5 roadmap. It adds
explicit selection commands and reuses existing page actions, modals,
SDK calls, and v1.4 inline confirmation. It does not add in-palette
secondary pickers or a global "pick any space" flow.

## Prior roadmap

The v1.3 and v1.4 cmdk designs deferred selection verbs to v1.5:

- Add selected to album
- Add selected to space
- Favorite selected
- Archive selected
- Delete selected

They also sketched multi-step palette state for "Add to album...", where
the palette would switch from command search into an album picker. That
multi-step picker is still the right larger direction, but it is not
needed for the first useful slice.

## Scope

In scope:

- Selection-aware `CommandContext` data.
- Explicit cmdk commands for:
  - Add selected to album...
  - Add selected to this space
  - Add selected to favorites
  - Archive selected
  - Delete selected
- Existing `AssetAddToAlbumModal` for album picking.
- Current-space add only on writable space pages while the page is in
  its existing asset-picking mode.
- Existing v1.4 inline confirm for delete.
- Focused unit/component tests and one route-level integration test per
  new page-wiring behavior.

Out of scope:

- In-palette album or space secondary pickers.
- A global "Add selected to space..." command that opens a space picker.
- Bridging every `CommandPaletteDefaultProvider` action into global cmdk.
- New toolbar layout or a new visible cmdk launcher in selection bars.
- Remove from favorites / unarchive selected commands. Those can follow
  after the add/archive-only verbs are stable.
- Tagging, metadata, stack, rotate, live-photo link, visibility, and job
  commands.

## In-palette secondary pickers

An in-palette secondary picker means the cmdk panel changes modes after a
command is chosen. Example: the user selects "Add selected to album...",
then the same palette turns into an album search view with a back affordance,
album rows, and an album-specific empty/loading state. Escape would return
to the command list, and Enter would add to the highlighted album.

v1.5A does not build that mode system. "Add selected to album..." closes
or yields the palette to the existing `AssetAddToAlbumModal`, which already
handles album selection and add semantics.

## Command inventory

| id | Label | Available when | Handler | Clears selection |
| --- | --- | --- | --- | --- |
| `cmd:selection_add_to_album` | Add selected to album... | Selection exists on a page that registered selection context | `modalManager.show(AssetAddToAlbumModal, { assetIds })` | No |
| `cmd:selection_add_to_current_space` | Add selected to this space | Space page, user can write, page is in existing `select-assets` mode, selection count is 1..`MAX_SPACE_ASSETS_PER_REQUEST` | Delegate to the page's existing add-to-space handler | Yes, by the existing space add flow |
| `cmd:selection_favorite` | Add selected to favorites | Selection exists, all selected assets are user-owned, page registered `onFavorite`, at least one selected asset is not already favorite | `updateAssets({ ids, isFavorite: true })`, mutate local selected asset refs, call page `onFavorite` | Yes, after success |
| `cmd:selection_archive` | Archive selected | Selection exists, all selected assets are user-owned, page registered `onArchive`, at least one selected asset is not already archived | `updateAssets({ ids, visibility: Archive })`, call page `onArchive` | Yes, after success |
| `cmd:selection_delete` | Delete selected | Selection exists, all selected assets are user-owned, page registered `onDelete` | v1.4 destructive two-Enter confirm, then delete selected assets | Yes, after success |

The current-space command is deliberately not a general space picker. It is
only "Add selected to this space". On the current codebase that must mean
the space page's `viewMode === 'select-assets'`; in normal space `view`
mode, selected assets are already inside the space, so showing "Add selected
to this space" would be a confusing duplicate/no-op.

## Context design

Extend `CommandContext` with a nullable `selection` subcontext:

```typescript
export interface SelectionCommandContext {
  assets: TimelineAsset[];
  selectedAssetIds: string[];
  ownedAssets: TimelineAsset[];
  ownedSelectedAssetIds: string[];
  isAllUserOwned: boolean;
  isAllFavorite: boolean;
  isAllArchived: boolean;
  isAllTrashed: boolean;
  onFavorite?: OnFavorite;
  onArchive?: OnArchive;
  onDelete?: OnDelete;
  onUndoDelete?: OnUndoDelete;
  addSelectedToCurrentSpace?: () => Promise<boolean>;
}

export interface CommandContext {
  routeId: string | null;
  params: Record<string, string>;
  album: AlbumContext | null;
  space: SpaceContext | null;
  selection: SelectionCommandContext | null;
  userId: string | null;
  isAdmin: boolean;
}
```

Add a `registerSelectionContext` helper beside `registerAlbumContext` and
`registerSpaceContext`. Pages call it with thunks that read
`assetMultiSelectManager` and page-local callbacks. The helper stores a
reactive selection context while the page is mounted and clears it on
unmount.

The context manager should return `selection: null` when the selected asset
list is empty. Handlers must still guard at activation time because the
selection can change while the palette is open.

## Page wiring

Wire selection context only on pages that can provide correct local update
callbacks.

Expected first pages:

- Photos page.
- Album detail page.
- Space detail page.
- Search results page.
- Tags page.
- Archive page for add-to-album, add-to-favorites, and delete. The
  "Archive selected" command remains hidden there because archive-page
  selections are already archived; "Unarchive selected" is deferred.

Each page should register only the callbacks it can support correctly. A
command that needs a missing callback should be hidden instead of doing an
SDK call that leaves the visible page stale.

The space page gets one extra page-local capability:

- When `viewMode === 'select-assets'` and `space.canWrite`, provide
  `addSelectedToCurrentSpace`.
- Otherwise omit `addSelectedToCurrentSpace`, which hides the command.

The handler should reuse the same limit check and add flow as the current
plus button: no selected assets, too many selected assets, SDK failures, and
the `SpaceAddAssets` event all behave the same way. That existing flow
refreshes the space, loads activity, clears selection, and returns to view
mode.

## Handler details

### Add selected to album

Use the existing modal:

```typescript
modalManager.show(AssetAddToAlbumModal, {
  assetIds: ctx.selection.selectedAssetIds,
});
```

The command is non-destructive. Canceling the modal changes nothing. A
successful add follows the modal/service's existing toast and event behavior.
Selection remains intact because this mirrors the current selection toolbar
action.

### Add selected to this space

Use the page-provided `addSelectedToCurrentSpace` callback instead of
duplicating space-page state in `command-items.ts`. The callback returns a
boolean success result:

- `true`: add completed and the page performed its normal refresh/clear.
- `false`: no-op, validation stop, cancel, or handled error; cmdk does not
  clear anything itself.

Availability must require:

- Current route has `ctx.space`.
- `ctx.space.canWrite` is true.
- `ctx.selection` exists.
- `ctx.selection.addSelectedToCurrentSpace` exists.
- `selectedAssetIds.length > 0`.
- `selectedAssetIds.length <= MAX_SPACE_ASSETS_PER_REQUEST`.

### Add selected to favorites

The command should match existing toolbar ownership rules: it is available
only when all selected assets are user-owned. Mixed owner selections should
not silently apply to only the owned subset.

Availability requires `onFavorite`; pages that cannot update visible state
do not get the command. Filter out assets that are already favorite before
calling the SDK. If the filtered id list is empty, hide the command. On
success:

- Mutate the selected asset refs to `isFavorite = true`.
- Call `onFavorite(ids, true)`.
- Show the same count toast as the toolbar action.
- Clear selection.

On SDK failure, show the existing error toast and keep selection.

### Archive selected

Same ownership and no-op rules as favorite. Availability requires
`onArchive`; pages that cannot update visible state do not get the command.
Use direct `updateAssets` or fix `archiveAssets()` before reusing it. The
current `archiveAssets()` helper returns ids even after catching an SDK
failure, so a command must not rely on that return value for success
semantics unless the helper is changed first.

On success:

- Call `onArchive(ids, AssetVisibility.Archive)`.
- Clear selection.

On failure, show the existing error toast and keep selection.

### Delete selected

Mark the command `destructive: true` so the existing v1.4 inline-confirm
state machine handles first Enter vs second Enter, Escape, query changes,
and stale pending confirm cancellation.

The actual delete call should return success/failure. Either update
`deleteAssets()` in `web/src/lib/utils/actions.ts` to return `true` on SDK
success and `false` after handled errors, or add a command-specific helper
with that contract. Existing callers may ignore the returned boolean.

Force-delete rules:

- Force when trash is disabled.
- Force when any selected asset is already trashed.
- Otherwise move to trash and expose undo through `onUndoDelete` when the
  page provides it.

On success:

- Call `onDelete(ids)` through the helper or directly.
- Wire undo through `onUndoDelete` for non-force deletes.
- Clear selection.

On failure, keep selection and do not call page update callbacks.

## Error handling and stale-state rules

- Command availability is an affordance, not the only guard. Every handler
  rechecks `ctx.selection` and required ids/callbacks at activation time.
- SDK failures show the existing localized error toast and do not clear
  selection.
- Modal cancel is not an error and does not clear selection.
- Mixed ownership hides favorite/archive/delete for parity with existing
  toolbar behavior.
- Add-to-album and add-to-current-space can use all selected ids.
- Add-to-current-space is hidden in normal space view mode to avoid adding
  already-in-space assets to the same space.
- Add-to-current-space is hidden for viewer/read-only space members.
- Add-to-current-space is hidden over `MAX_SPACE_ASSETS_PER_REQUEST`.
- If navigation or page unmount clears the registered context while cmdk is
  open, activation becomes a no-op.
- If selection changes while cmdk is open, activation uses the live context
  snapshot, not the stale row text.

## TDD requirement

Implementation must use TDD. No production code for this feature is written
until a focused failing test exists and has been run.

Use vertical red-green-refactor slices:

1. Write one failing test for one behavior.
2. Run the focused test and confirm it fails for the expected reason.
3. Write the smallest implementation that makes that test pass.
4. Run the focused test again and confirm it passes.
5. Run the relevant neighboring tests before moving to the next behavior.
6. Refactor only while tests are green.

Do not write all tests first and then implement the whole feature. Do not
write implementation first and backfill tests. Each implementation commit
should have its own red/green evidence in the working notes or PR body.

## Test plan

### Context manager tests

Add or extend `web/src/lib/managers/command-context-manager.spec.ts`.

Tests:

- `selection` is `null` when no assets are selected.
- Selection snapshot exposes `assets`, `selectedAssetIds`, `ownedAssets`,
  `ownedSelectedAssetIds`, `isAllUserOwned`, `isAllFavorite`,
  `isAllArchived`, and `isAllTrashed`.
- Registered callbacks are present in the selection context.
- Clearing selection updates the context to `selection: null`.
- Unmount cleanup clears selection context.
- Route changes cannot leak stale album/space context into unrelated routes.

### Command inventory and availability tests

Extend `web/src/lib/managers/command-items.spec.ts`.

Tests:

- All new command ids are unique and have label/description keys.
- Add-to-album appears for any non-empty registered selection.
- Add-to-current-space appears only with writable `ctx.space`,
  `addSelectedToCurrentSpace`, non-empty selection, and selected count within
  `MAX_SPACE_ASSETS_PER_REQUEST`.
- Add-to-current-space is hidden for read-only/viewer spaces.
- Add-to-current-space is hidden when the space page has not registered its
  add callback, which covers normal space view mode.
- Favorite is hidden for missing `onFavorite`, mixed ownership, or
  already-favorite selections.
- Archive is hidden for missing `onArchive`, mixed ownership, or
  already-archived selections.
- Delete is hidden for missing `onDelete` or mixed ownership and marked
  `destructive: true`.

### Handler tests

Extend `command-items.spec.ts` or introduce a small helper spec if handler
logic is extracted.

Tests:

- Add-to-album opens `AssetAddToAlbumModal` with the exact selected ids and
  does not clear selection.
- Add-to-current-space delegates to `addSelectedToCurrentSpace` and does not
  perform its own duplicate SDK call.
- Favorite calls `updateAssets` with only ids that are not already favorite,
  mutates selected asset refs, calls `onFavorite`, and clears selection after
  success.
- Favorite SDK failure shows an error and keeps selection.
- Archive calls `updateAssets` with only ids that are not already archived,
  calls `onArchive`, and clears selection after success.
- Archive SDK failure shows an error and keeps selection.
- Delete first activation starts inline confirm and does not call the delete
  helper.
- Delete second activation calls the delete helper.
- Delete success clears selection and calls page callbacks.
- Delete failure keeps selection and does not call page callbacks.
- Delete force flag is true when trash is disabled or any selected asset is
  already trashed.

### Page wiring tests

Add focused route/component tests where page tests already exist:

- Photos page registers selection callbacks for favorite/archive/delete.
- Search page registers callbacks that update `searchResultAssets` correctly.
- Space page registers `addSelectedToCurrentSpace` only in `select-assets`
  mode and only for writable users.
- Space page add command respects the max-selected limit.
- Space page add command triggers the same refresh/clear path as the existing
  plus button.

### Cmdk manager/component regression tests

Existing v1.4 confirm tests should remain green. Add only the missing
selection-specific assertions:

- Selection delete row uses pending confirm styling through the existing
  destructive path.
- Escape cancels pending delete confirm without closing the palette when the
  existing v1.4 conditions say it should.
- Query changes cancel pending delete confirm.

### E2E or smoke coverage

Add one browser-level smoke test if the existing suite has a stable fixture
for selected assets:

- Select assets on a writable space add-assets flow, open cmdk, run "Add
  selected to this space", and verify the page exits add mode after success.

If the E2E fixture is too brittle, keep this as a manual verification item in
the implementation plan and rely on route-level component tests for CI.

## Edge cases

- Empty selection while cmdk is open: command handler no-ops.
- Selection changes after the result row was rendered: activation uses the
  current selected ids.
- Asset already favorite: favorite command omits it from the SDK update.
- All selected assets already favorite: favorite command is hidden.
- Asset already archived: archive command omits it from the SDK update.
- All selected assets already archived: archive command is hidden.
- Mixed owned/shared selection: favorite/archive/delete are hidden.
- Non-owned selection: favorite/archive/delete are hidden.
- Add-to-album modal canceled: selection remains.
- Add-to-album service failure: modal/service handles toast; selection remains.
- Space viewer opens cmdk while assets are selected: add-to-current-space is
  hidden.
- Space editor selects more than `MAX_SPACE_ASSETS_PER_REQUEST`: add-to-current
  space is hidden.
- Delete confirm times out: no delete call.
- Delete SDK failure: no clear, no page callback.
- Non-force delete with undo callback: undo restores through the existing
  toast action.
- Navigation while pending delete confirm: existing v1.4 close-on-navigation
  clears pending confirm and page selection manager clears selection.

## Delivery shape

Target branch/worktree:

- Branch: `cmdk-search-v1-5`
- Worktree: `.claude/worktrees/cmdk-search-v1-5`

Expected implementation commits, each kept green:

1. `feat(web): add cmdk selection context`
2. `feat(web): add cmdk add-selected commands`
3. `feat(web): add cmdk favorite and archive selection commands`
4. `feat(web): add cmdk delete selected command`
5. `feat(web): wire selection context on timeline pages`

The implementation plan can split or merge commits if TDD slices reveal a
cleaner shape, but each slice must preserve the red-green-refactor workflow.

## Deferred follow-ups

- In-palette album picker for "Add selected to album...".
- In-palette space picker for "Add selected to space...".
- Full `CommandPaletteDefaultProvider` bridge.
- Remove from favorites and unarchive selected commands.
- Selection-toolbar cmdk launcher or shortcut hint, if discoverability becomes
  a problem.
- Additional page contexts beyond the pages listed here.
